#!/usr/bin/env sh

# Lint + format staged .ts files (runs from repo root so lint-staged stash works)
npx lint-staged

# TypeScript type check (catches errors that per-file linting misses)
cd ts-src || exit 1
npm run typecheck
