# 核心依赖
httpx>=0.27.0
markdownify>=0.5.0
Brotli>=1.0.0

# Windows 文件时间设置（可选，Windows 用户推荐）
win32-setctime>=1.0.0; sys_platform == 'win32'

# 自动登录（可选，推荐）
playwright>=1.40.0

# 从浏览器自动提取 cookies（可选）
# browser-cookie3>=0.19.0