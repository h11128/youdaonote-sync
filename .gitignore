# IDE
.idea/
.vscode/
*.iml
.history

# Node.js
node_modules/

# Build artifacts
build/
dist/
ts-src/dist/

# Test coverage reports
coverage/
ts-src/coverage/

# Secrets / local machine config (never commit)
.env
.env.local
.env.*.local
*.pem
*.key

# Project config (user-specific runtime data — use platform SOT, not the repo)
config/
config.legacy-backup/
config.migrated-*/

captured_api_requests.json
logs/
backups/
*.backup.*
test-pull/

# Export directory (user-generated note files)
youdaonote-export/

# Local debug scripts
.local-scripts/

# Performance profiling
profiles/

# Python legacy
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

.cursor/

# vendored by myforge/scripts/install-hooks.sh for the pre-commit git-crypt guard
/target/release/git-crypt-tool
/target/release/git-crypt-tool.exe
