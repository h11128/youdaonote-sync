# Examples

Templates to copy into your runtime config directory (not the repo).

| File | Copy to |
|------|---------|
| [config.example.json](./config.example.json) | `{SOT}/config.json` |
| [.env.example](./.env.example) | optional repo or shell `.env` |

SOT path and fields: [configuration guide](../docs/guides/configuration.md).
