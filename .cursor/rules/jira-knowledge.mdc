---
description: Jira project management — tickets, epics, sprints, milestones, estimation, PDMOB fields
alwaysApply: false
---

# Jira Project Management

**环境变量**:
- `ATLASSIAN_EMAIL`: Email for Jira auth
- `ATLASSIAN_TOKEN`: API token (NOT password)

**本地指南**: `~/Documents/pedregal-local-docs/dasher-comms-jira-guide.md`

**里程碑设计**:
- High-level for manager (Phase 1/2/3), not per-API detail
- Use subtasks for specifics; list blockers separately
- Format: Table — Phase | ETA | Scope | Status

**估时规则**:
- Based on historical data
- Consider parallelism, weekends, holidays
- Conservative = optimistic + 1 week buffer
- Config tasks can batch

**API 用法**:
- Description uses ADF format
- subtasks query: `parent=EPIC-KEY`
- Change status via transition API

**PDMOB 必填字段**: 详见本地指南（上方路径），包含 summary 格式、issuetype、labels、assignee 等。
