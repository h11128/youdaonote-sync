---
description: Code review prompt template — for independent review in a new conversation after large changes
alwaysApply: false
---

# 代码审查提示词模板

> 大改动完成后，在**新对话**中使用此模板做独立审查。
> 新对话的上下文与开发对话完全隔离，能避免"自审退化为确认自己的理解"。

## 使用方法

1. 开发完成并提交后，开一个新的 Cursor 对话
2. 复制下方「审查提示词」，替换 `{{占位符}}` 后粘贴
3. 审查结果作为新一轮修复的输入

## 触发条件

以下情况 **必须** 用新对话审查（不能在开发对话中自审）：

- 改动涉及 **5+ 文件**
- 改动涉及 **核心算法 / 状态机 / 并发逻辑**
- 改动涉及 **数据迁移 / 接口变更**
- 改动行数 **+200 行以上**

## 审查提示词

```text
请审查以下改动。你是独立的审查者，不是开发者——对每个问题给出具体的文件名、行号和原因。

## 改动范围

{{简述改动目的，例如：修复睡眠检测的准确性，涉及 18 个文件}}

## 改动文件

{{列出主要文件，或用 git diff --stat 的输出}}

## 审查维度

按以下 4 个维度逐一检查，每个维度给出 ✅ 通过 / ⚠️ 需关注 / ❌ 必须修复：

### 1. 算法正确性
- 边界条件：空输入、零值、极大值、负数
- 时间窗口 / 过期逻辑：无新输入时旧数据是否正确过期？
- 状态机：所有状态转换是否都有对应的测试？是否有不可达状态？
- 数值精度：浮点比较是否用了容差？整数溢出风险？

### 2. 运行时特征
- 调用频率：这段代码多久执行一次？（每 2 秒 vs 每天一次，性能要求完全不同）
- 线程安全：共享可变状态是否有同步机制？
- 内存：是否有只增不减的集合？是否缺少清理逻辑？
- 资源泄漏：打开的连接 / 文件 / 订阅是否都有对应的关闭？

### 3. 架构一致性
- 依赖方向：低层模块是否意外依赖了高层模块？
- 职责划分：一个类 / 文件是否承担了多个不相关的职责？
- 已有模式：改动是否遵循了项目中同类代码的既有模式？

### 4. 向后兼容
- 接口变更：公共 API / 函数签名的变化是否更新了所有调用方？
- 数据兼容：数据库 / 文件格式变化是否有迁移逻辑？
- 配置：新增的配置项是否有合理的默认值？

### 5. 功能对等性（仅限重写 / 移植项目）
- 旧系统的每个公开函数 / 入口在新系统有对应实现或明确的"推迟/丢弃"标注？
- 旧系统 engine 的每个步骤（init → scan → classify → refine → execute → cleanup）在新 engine 中有对应调用？
- 新写的每个模块至少被一个上层模块导入并调用（不能只被测试导入）？
- 哈希 / 加密算法与旧系统一致，或有文档说明为什么不同？
- 临时方案（"先用 X 后换 Y"）是否都有 TODO 标记？

## 输出格式

对每个发现，给出：
- **严重性**: P0（必须修复）/ P1（建议修复）/ P2（改进建议）
- **位置**: 文件名:行号
- **问题**: 一句话描述
- **建议**: 具体的修复方向
```

## 补充说明

- 审查不能替代测试。审查发现的问题仍需写测试来验证修复。
- 审查模板是起点，不是终点。根据项目特点可以增删维度。
- 同一个 agent 的开发对话和审查对话**不要**共享上下文——这是本模板存在的核心原因。
