---
description: Rust code taste, idioms, and best practices — triggered when writing/reviewing Rust code
globs:
  - "**/*.rs"
  - "**/Cargo.toml"
alwaysApply: false
---

# Rust Idioms & Code Taste

## Type System — Parse, Don't Validate

- 结构已知时用强类型 struct + `serde::Deserialize`，不要全程 `serde_json::Value`
- `serde_json::Value` 仅用于结构真正未知的场景（schema validation 等）
- 项目已有类型（如 `AnalysisEnvelope`）→ 直接 deserialize，不要手写 `.get("field")`
- **封闭枚举用 tagged enum**：当 YAML/JSON 的 `type` 字段只有有限几种值（如 condition 10 种、indicator 5 种），用 `#[serde(tag = "type")]` enum 反序列化。serde 自动拒绝未知 type、缺字段、类型错误——不要手写 `.get("type").and_then(|v| v.as_str())` match chain

## Make Illegal States Unrepresentable

- 用 enum 表达互斥状态，不要 bool 组合（`is_cluster: bool` → `enum AuditStrategy`）
- 用 newtype 区分语义不同的值（不要 `String` 一把梭）
- `Option<T>` 表达"可能没有"，不要用空字符串/零值做 sentinel

## 函数设计

- 计算和 IO 分离：纯计算函数返回数据结构，IO 层负责打印/写文件
- 避免 `&mut Vec<T>` 做 out parameter → 优先返回 `Vec<T>`，调用方 `extend`
- 函数签名是文档：`Option<&T>` 优于 `&Option<T>`，`&'static str` 优于不必要的生命周期绑定

## 命名常量

- 阈值、大小限制、显示行数等必须是命名常量，不要魔法数字
- 常量命名用 `SCREAMING_SNAKE_CASE`，放在使用它的函数附近或模块顶部

## 错误处理

- 用 `anyhow::Result` 做应用层错误，`thiserror` 做库层错误
- `unwrap()` 禁止出现在非测试代码中（已在 Cargo.toml 配为 warn）
- `?` 优于 match + early return

## Clippy 纪律

- `clippy::pedantic` 全开，审慎 allow 附理由（见 Cargo.toml `[lints.clippy]`）
- 不允许 `#[allow(clippy::...)]` 无注释——每个 allow 必须有理由
- `cargo clippy --all-targets` 必须 0 warnings

## 代码组织

行数阈值遵循 `module-organization.mdc` 的统一定义：

- **>300 lines** → 审查职责是否单一，评估是否需要拆分
- **>600 lines** → 必须提出拆分方案
- 例外：如果文件中所有函数结构完全一致、职责单一（如 N 个 `render_*_section`），可放宽到 ~450 行，但必须在文件头部注释说明为什么不拆
- 拆分模式：大文件 `foo.rs` → `foo/mod.rs`（facade，只含公共 API 和编排）+ `foo/xxx.rs`（各职责子模块）
- 新增功能时，如果目标文件已接近 300 行，必须先评估是否需要拆分，再写新代码
- `items_after_statements`：函数内的 struct/fn 定义放在 let 语句之前
- `format_push_string`：用 `write!` 代替 `push_str(&format!(...))`

## `serde_json::Value` 边界规则

`serde_json::Value` / `serde_yaml::Value` 只允许在以下入口使用：
- LLM 返回的 JSON 解析（`annotation/engine/response.rs`）
- DB 读取（`rusqlite::Row::get`）
- 外部 chart JSON 解析入口

内部代码 MUST 使用强类型 struct。从 `Value` 到 struct 的转换在解析边界做一次。禁止 `Value` 在函数间传递后再 `.get("string")` 做运行时查找。

## `Option` 语义清晰化

`Option<f64>` 在 Indicator 返回中：`None` = 该天数据缺失（排除该天），`Some(0.0)` = 事件未发生。

新增返回 `Option` 的公共函数 MUST 在 doc comment 中说明 `None` 的含义。禁止 `None` 同时表示"缺失"和"默认值"。

## 参考资源

- Effective Rust (David Drysdale) — 类型系统、trait、工具链
- Rust API Guidelines — 命名、类型安全、可预测性
- Microsoft Pragmatic Rust Guidelines — 工程实践
